P = phantom(256);
figure;
colormap(gray);
imagesc(P);
axis image;
[U,S,V] = svd(P);
figure;
SLess = S * [ones(100),zeros(size(S,1)-100)];
% les plus grandes valeurs singulière
PRecomp = U * SLess * V.';